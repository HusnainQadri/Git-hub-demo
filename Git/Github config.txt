Gernerate SSH Key locally: $ ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
ADD device to your guthub with .pub key (User/.ssh/[2 files one is .pub]):
Configure your private key now: 1: eval "$(ssh-agent -s)"
                                2: ssh-add ~/.ssh/id_ed25519
